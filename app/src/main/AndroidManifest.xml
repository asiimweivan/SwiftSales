<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission for barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".SwiftSalesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftSales">

        <!-- SPLASH — Launcher entry point -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.SwiftSales.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.language.LanguageActivity" />
        <activity android:name=".ui.onboarding.OnboardingActivity" />
        <activity android:name=".ui.auth.SignUpActivity" />
        <activity android:name=".ui.auth.SignInActivity" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" />
        <activity android:name=".ui.auth.BusinessSetupActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity android:name=".ui.inventory.InventoryActivity" />
        <activity android:name=".ui.pos.POSActivity" />
        <activity android:name=".ui.pos.CheckoutActivity" />
        <activity android:name=".ui.reports.ReportsActivity" />
        <activity android:name=".ui.staff.StaffActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.orders.OrdersActivity" />
        <activity android:name=".ui.notifications.NotificationsActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />

        <!-- ZXing barcode scanner activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />

    </application>

</manifest>